name: Set up governed npm
description: Install the npm release required by DocBlocks dependency policy.

runs:
  using: composite
  steps:
    - name: Install npm 11.19.1
      shell: bash
      run: npm install --global npm@11.19.1

    - name: Verify npm version
      shell: bash
      run: npm --version
